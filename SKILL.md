---
name: tape-collage-studio
description: 将照片、产品、人物、宠物、建筑或文字主题制作成具有真实和纸胶带质感的中文友好型拼贴画，也可忠实保留原图并与胶带重构并置。用户提到胶带拼贴、和纸胶带、胶带画、拼贴手账、照片转拼贴、保留原图、社媒拼贴海报、小红书拼贴或需要修改已有胶带拼贴时使用。
---

# 胶带拼贴工作室

把一个明确主体提炼成克制、可辨认、具有真实手工触感的胶带拼贴。优先交付成品图；除非用户只要提示词，否则不要只给描述。

## 开始前

1. 阅读 [references/style-system.md](references/style-system.md)。
2. 需要生成、编辑或返修时，再阅读 [references/prompt-recipes.md](references/prompt-recipes.md)。
3. 照片、产品或人物输入必须先逐张检查，确认主体、姿态、构图、必须保留项和禁止改动项。
4. 仅在判断材质或抽象程度有歧义时查看 `assets/style-references/`；参考其胶带质感与留白，不复制其中主体。

## 选择工作模式

按用户真实意图选择一种模式；明确要求优先于默认值。

- **胶带重构**：把输入照片中的主体重新搭建成胶带画，原照片不出现在成品中。默认模式。
- **原图并置**：用户说“保留原图、原照片也要、照片不要改”时，忠实保留照片并配一个胶带重构区。
- **文字创作**：根据一句主题或情绪生成完整胶带拼贴作品。
- **透明素材**：用户要抠图、透明 PNG 或方便二次设计时，只生成独立胶带主体，背景必须是真透明 RGBA。
- **定向返修**：只修用户指出的问题，保留所有已认可内容。

不要把“以照片为参考”误判为“保留原照片”。

## 先确定输出规格

- 用户指定比例、平台、文案或留白位置时，严格执行。
- 未指定时：普通作品用 `3:4`；小红书用 `3:4`；抖音/快手竖版用 `9:16`；方形头像或封面用 `1:1`。
- 电商或社媒作品要为标题、产品信息或后续排版预留安全留白，但不要擅自生成营销文案。
- 默认不加文字。用户要求文字时，逐字照写；中文、数字、品牌名和标点都视为不可改动项。
- 精确文字优先在生成后确定性叠加，不依赖模型自由绘字。普通作品使用 `scripts/add_exact_text.py`；原图并置使用 `scripts/compose_direct_split.py`。

## 胶带重构与文字创作

1. 只保留 1–3 个最重要的识别锚点，例如轮廓、姿态、耳朵、瓶型、建筑天际线或标志性色块。
2. 从输入提取 3–6 个主色，默认低饱和，最多一个强强调色。产品包装要求保色时，不擅自柔化品牌色。
3. 根据复杂度使用约 8–24 块宽或中宽胶带构成主体；优先完整结构面，不做碎纸屑马赛克。
4. 用胶带层叠、明中暗色面、负形和窄胶带条表达结构，禁止用铅笔、墨线或数字矢量描边偷懒。
5. 背景使用干净暖白纸张；纸纤维可见但克制，禁止脏污、重度做旧和杂乱桌面道具。
6. 默认保留大量留白。主体完整且清晰，不因“极简”缩成难以识别的小图标。
7. 调用内置图像生成工具完成成品。照片转换时正确传入目标图；纯文字创作不要虚构参考图。

## 原图并置

该模式必须把“生成”和“排版”分开，避免模型重绘原照片。

1. 先运行：

   ```bash
   python scripts/compose_direct_split.py --photo <照片路径> --plan
   ```

2. 依据输出的版面尺寸，只生成一个无文字、真透明 RGBA 的胶带主体；不要生成纸张、相框或完整拼版。
3. 再用同一脚本合成：

   ```bash
   python scripts/compose_direct_split.py \
     --photo <照片路径> \
     --motif <透明胶带主体.png> \
     --output <成品.png> \
     --caption "<用户指定文字>"
   ```

4. 默认画幅 `3:4`、原图与纸面约各占一半。竖图默认左右排，横图或方图默认上下排。
5. 原照片像素不得生成式重绘、修脸、调色、拉伸或添加滤镜。必要裁切最多移除原图面积的 20%；可能伤及主体时改用不裁切。
6. 排版位置、字号、纸纹、阴影和留白问题只能本地修正，不能浪费一次生成去重做整张图。

## 透明素材

- 只输出一个完整胶带主体，四周留足透明空间。
- 必须检查 alpha 通道；拒绝白底、纸色矩形、棋盘格假透明和边缘色晕。
- 允许胶带内部的叠压阴影，不允许整块画布阴影。

## 文字处理

- 用户没要求文字时，保持无字，不自动起英文标题。
- 用户给出文案时，不改写、不翻译、不补标点、不加副标题。
- 中文标题默认使用技能内置中文字体；若没有内置字体，则从系统中寻找可覆盖全部字符的中文字体。
- 先生成无字图，再运行：

  ```bash
  python scripts/add_exact_text.py \
    --image <无字成品.png> \
    --output <最终成品.png> \
    --text "<精确文字>" \
    --position lower-left
  ```

- 若用户明确要手写字、印章感或文字本身也是画面主体，可让模型参与造型；仍需逐字检查。

## 返修策略

- 先列出必须保持不变的项目，再只修改一个失败属性。
- 材质不像胶带或主体不可辨认时，最多做一次针对性生成返修。
- 大小、位置、裁切、留白、文字和纸张问题优先通过本地脚本修正。
- 不因修一个问题而改变已认可的主体、姿态、比例、品牌色、背景或文字。

## 交付前检查

- 主体在缩略图尺寸下仍可辨认，识别锚点没有丢失。
- 第一眼读作真实和纸胶带：有纤维、半透明叠压、轻微褶皱、自然撕边和浅接触阴影。
- 画面仍是平面手账拼贴，不是立体纸雕、塑料贴纸、矢量插画或碎屑马赛克。
- 纸面干净、留白充足，主体和文字不贴边。
- 不出现无关票据、邮票、标签、剪刀、胶带卷、手、桌面杂物、乱码、水印或签名。
- 原图并置时，确认照片未被重绘或拉伸，裁切不超过 20%，胶带主体背景是真透明。
- 所有指定文字逐字准确；没要求文字时，成品无字。

生成结果不合格时，指出具体失败项并进行一次定向修正；不要盲目连续重抽。
